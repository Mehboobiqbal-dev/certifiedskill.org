name: Trigger Scrape API

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel API
        run: curl -f https://certifiedskill.org/api/scrape || exit 1